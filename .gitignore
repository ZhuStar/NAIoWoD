node_modules/
# dist/ holds generated build artifacts; the one committed deliverable is the
# readable single-file build, kept in sync by test/build.test.ts.
dist/*
!dist/naiowod.ts
*.log
.DS_Store
bun.lockb
# Session working scaffolding: standalone artifact-check copies, e2e smoke
# scripts, throwaway notes. Regenerated by the verification battery; never a
# deliverable (the artifact is dist/naiowod.ts, the tests live in test/).
scratchpad/
